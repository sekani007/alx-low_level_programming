#include "main.h"
/**
 * mul - A program that multiplies two integers
 * @a: integer for the multiplication
 * @b: integer for the multiplication
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
