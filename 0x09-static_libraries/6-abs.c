#include "main.h"
/**
 * _abs - A program that prints an absolute value
 * @n: The integer to be checked
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
